Typescript exmaples with decorators are located in `decorators/src`

Open decorators folder via terminal and execute `npm run start:dev`

In `decorators/src/index.ts` you can change imports to see different examples 
